import React from 'react';
import AnalyticsContent from '../analytics/index';

export default function Dashboard() {
    // Render the Analytics content directly within the Dashboard tab
    return <AnalyticsContent />;
}